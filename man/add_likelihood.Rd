% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{add_likelihood}
\alias{add_likelihood}
\title{Cache the likelihood fit on a bpm object}
\usage{
add_likelihood(object, ...)
}
\arguments{
\item{object}{A \code{bpm} object.}

\item{...}{Currently unused.}

\item{data}{Optional data frame of the development sample. Required when
\code{object} was fitted with \code{model = FALSE}; ignored otherwise.}
}
\value{
The same \code{bpm} object with the \code{likelihood_fit} field populated.
}
\description{
Fits the unpenalised (flat-prior) logistic regression, caches the MLE and
observed Fisher information on the object, and returns the updated object.
After calling this, \code{\link[=likelihood]{likelihood()}} works instantly with no recomputation.
}
\details{
This function is pipe-friendly: the object is the first argument and the
updated object is returned, so it slots naturally into a \verb{|>} chain:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{fit <- bpm(...) |> add_likelihood() |> add_projection()
}\if{html}{\out{</div>}}

For the \code{flat()} prior this is a no-op (likelihood == posterior already);
the object is returned unchanged.
}
\seealso{
\code{\link[=likelihood]{likelihood()}}, \code{\link[=posterior]{posterior()}}, \code{\link[=add_projection]{add_projection()}}
}
