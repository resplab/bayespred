% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict.bpmfit}
\alias{predict.bpmfit}
\title{Predict from a bpmfit model}
\usage{
\method{predict}{bpmfit}(
  object,
  newdata,
  type = c("response", "link", "terms"),
  method = c("pm", "pe", "pm_mackay"),
  se.fit = FALSE,
  interval = NULL,
  dispersion = NULL,
  na.action = na.pass,
  ...
)
}
\arguments{
\item{object}{A \code{bpmfit} object.}

\item{newdata}{A data frame of new observations. If omitted, predictions are
made on the training data (requires \code{model = TRUE} at fit time).}

\item{type}{Character string controlling the output scale (same as
\code{\link[stats:predict.glm]{stats::predict.glm()}}):
\describe{
\item{\code{"response"}}{(default) Predicted probability, controlled by \code{method}.}
\item{\code{"link"}}{Linear predictor \eqn{\eta = x^\top\hat\beta}.
\code{method} is ignored.}
\item{\code{"terms"}}{Not implemented in v1.}
}}

\item{method}{Character string selecting the Bayesian computation. Only
relevant when \code{type = "response"}:
\describe{
\item{\code{"pm"}}{(default) Posterior mean via 30-point Gauss-Hermite quadrature.}
\item{\code{"pe"}}{Plug-in estimate: \code{plogis(X \%*\% beta_hat)}. Equivalent to
\code{predict.glm(..., type = "response")}.}
\item{\code{"pm_mackay"}}{MacKay (1992) closed-form approximation to the PM.}
}}

\item{se.fit}{Logical. If \code{TRUE}, return a list with elements \code{fit},
\code{se.fit}, and \code{residual.scale}, matching \code{\link[stats:predict.glm]{stats::predict.glm()}}.
\code{se.fit} is on the same scale as \code{type}: for \code{"link"} it is
\eqn{\sqrt{x^\top V x}}; for \code{"response"} it is the delta-method
approximation \eqn{\hat\mu(1-\hat\mu)\sqrt{x^\top V x}}. Default \code{FALSE}.}

\item{interval}{Either \code{NULL} (point predictions only, the default), \code{0}
(return \code{fit} and \code{se.fit} without bounds), or a numeric scalar in (0, 1)
giving credible interval coverage (e.g. \code{0.95}). Intervals are computed on
the linear-predictor scale (normal approximation) and back-transformed via
\code{plogis} for \code{type = "response"}. Cannot be combined with \code{se.fit = TRUE}.}

\item{dispersion}{Ignored. Included for compatibility with
\code{\link[stats:predict.glm]{stats::predict.glm()}}; the binomial dispersion is always 1.}

\item{na.action}{Function to handle \code{NA}s in \code{newdata}. Default
\link[stats:na.pass]{stats::na.pass}, matching \code{\link[stats:predict.glm]{stats::predict.glm()}}.}

\item{...}{Currently unused.}
}
\value{
\itemize{
\item If \code{interval = NULL} and \code{se.fit = FALSE}: a numeric vector.
\item If \code{interval = 0}: a data frame with columns \code{fit} and \code{se.fit}
only — no bounds. \code{se.fit} is on the same scale as \code{type}.
\item If \code{interval} is in (0, 1): a data frame with columns \code{fit},
\code{lwr}, \code{upr}, and \code{se.fit} (on the same scale as \code{type}).
\item If \code{se.fit = TRUE}: a list with elements \code{fit}, \code{se.fit}, and
\code{residual.scale} (1 for binomial).
}
}
\description{
Computes predictions from a fitted \code{bpmfit} object. The interface is
intentionally compatible with \code{\link[stats:predict.glm]{stats::predict.glm()}}: \code{type} controls the
output scale and \code{se.fit} requests standard errors. The additional \code{method}
argument selects the Bayesian computation used for \code{type = "response"}.
}
\details{
When \code{newdata} is omitted, predictions are computed on the training data
using the stored model frame (requires \code{model = TRUE} at fit time). To
predict on new data from a portable, self-contained object, extract the
posterior with \code{\link[=posterior]{posterior()}} and call \code{\link[=predict.bpm]{predict.bpm()}} directly.

The marginal posterior of the linear predictor is approximated as
\deqn{\eta_i \mid \text{data} \approx N(x_i^\top\hat\beta,\; x_i^\top V x_i).}
All credible intervals and standard errors are derived from this
approximation. For \code{type = "response"} the PM point estimate lies inside
but not at the centre of the interval (Jensen's inequality).
}
\examples{
set.seed(1)
d <- data.frame(x = rnorm(200), y = rbinom(200, 1, 0.3))
fit <- bpmfit(y ~ x, data = d, prior = log_f(m = 2))

# glm-style
predict(fit, data.frame(x = 0.5), type = "response")
predict(fit, data.frame(x = 0.5), type = "link")
predict(fit, data.frame(x = 0.5), type = "link",    se.fit = TRUE)
predict(fit, data.frame(x = 0.5), type = "response", se.fit = TRUE)

# Bayesian extensions
predict(fit, data.frame(x = 0.5), method = "pm",   interval = 0.95)
predict(fit, data.frame(x = 0.5), type = "link",   interval = 0.95)

}
