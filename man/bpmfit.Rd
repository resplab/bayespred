% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bpm.R
\name{bpmfit}
\alias{bpmfit}
\title{Fit a Bayesian prediction model}
\usage{
bpmfit(
  formula,
  data,
  family = binomial(link = "logit"),
  prior = log_f(m = 2),
  model = TRUE,
  ...
)
}
\arguments{
\item{formula}{A model formula (same syntax as \code{\link[stats:glm]{stats::glm()}}).}

\item{data}{A data frame containing the variables in \code{formula}.}

\item{family}{A family object. Only \code{binomial(link = "logit")} is supported
in v1; any other value raises an informative error.}

\item{prior}{A prior specification created by \code{\link[=flat]{flat()}}, \code{\link[=jeffreys]{jeffreys()}},
\code{\link[=log_f]{log_f()}}, or \code{\link[=bridge]{bridge()}}. Default: \code{log_f(m = 2)}.}

\item{model}{Logical. If \code{TRUE} (default), store the model frame on the
returned object (used by \code{predict()} when \code{newdata} is absent).}

\item{...}{Currently unused.}
}
\value{
An object of class \code{"bpmfit"} with components:
\describe{
\item{\code{posterior}}{A \code{bpm} object (see \code{\link[=posterior]{posterior()}}) containing the
deployable posterior: \code{coefficients} (MAP estimate), \code{vcov}
(Laplace-approximated posterior covariance), \code{family}, \code{terms},
\code{contrasts}, and \code{xlevels}. This is everything needed for prediction
with base R only.}
\item{\code{prior}}{The prior specification object.}
\item{\code{formula}}{The model formula.}
\item{\code{call}}{The matched call.}
\item{\code{model}}{The model frame (if \code{model = TRUE}).}
\item{\code{fit_method}}{Internal tag identifying the fitting backend.}
}
}
\description{
Fits a logistic regression model under one of four shrinkage priors and
returns a self-contained \code{bpmfit} object. The deployable \code{bpm} posterior
(coefficients, covariance, link) is stored as \verb{$posterior} and can be
extracted with \code{\link[=posterior]{posterior()}}. Both \code{bpmfit} and \code{bpm} objects accept
\code{\link[=predict]{predict()}}; the latter always requires \code{newdata}.
}
\examples{
set.seed(1)
d <- data.frame(x = rnorm(200), y = rbinom(200, 1, 0.3))
fit <- bpmfit(y ~ x, data = d, prior = log_f(m = 2))
predict(fit, data.frame(x = 0.5))

# Extract the deployable posterior and predict from it
post <- posterior(fit)
predict(post, data.frame(x = 0.5))

}
\seealso{
\code{\link[=predict.bpmfit]{predict.bpmfit()}}, \code{\link[=predict.bpm]{predict.bpm()}}, \code{\link[=posterior]{posterior()}},
\code{\link[=flat]{flat()}}, \code{\link[=jeffreys]{jeffreys()}}, \code{\link[=log_f]{log_f()}}, \code{\link[=bridge]{bridge()}}
}
