% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/projection.R
\name{project_pm}
\alias{project_pm}
\title{Project a bpm model onto a simplified linear predictor (PM method)}
\usage{
project_pm(object, ...)
}
\arguments{
\item{object}{A \code{bpmfit} object.}

\item{...}{Currently unused.}

\item{formula}{Formula for the projection model. \code{NULL} (default) uses the
same formula as \code{object}. A one- or two-sided formula (LHS is ignored)
overrides this, e.g. \code{~ age + sex}.}

\item{data}{Data frame used to compute PM predictions and fit the projection.
Can be the original development sample or a different dataset (e.g. local
data from a new site). When omitted, the model frame stored on \code{object} is
used (requires \code{model = TRUE} at fit time).}

\item{family}{Family object for the projection fit. \code{NULL} (default) reuses
\code{object$family}.}
}
\value{
An object of class \code{"bpmproj_pm"} with elements \code{coefficients},
\code{terms}, \code{contrasts}, \code{xlevels}, \code{family}, and \code{call}.
}
\description{
Computes posterior-mean (PM) predictions on the development sample and
regresses them onto a design matrix, producing a standalone \code{bpmproj_pm}
object with no covariance matrix. All three aspects of the projection model
default to the main \code{bpm} fit but can be overridden independently:
}
\details{
\itemize{
\item \strong{\code{formula}} — defaults to the main model's formula (self-projection).
Supply a simpler or different formula to project onto a reduced predictor
set, e.g. \code{~ age + sex}.
\item \strong{\code{family}} — defaults to \code{object$family} (same link as the main model).
Override to project onto a different link function.
\item \strong{\code{data}} — the sample used both to compute PM predictions (always via
the main model's coefficients and vcov) and to fit the projection formula.
Can be the original development sample, or a different dataset entirely —
for example, local data from a new site, enabling domain-specific
projection without refitting the main model. When omitted, the model frame
stored on \code{object} is used (requires \code{model = TRUE} at fit time).
}

The returned object is self-contained and can be saved and deployed with
\code{\link[=predict.bpmproj_pm]{predict.bpmproj_pm()}} using base R only — no dependency on the original
fit or on \code{mgcv} / \code{brglm2}.
}
\seealso{
\code{\link[=predict.bpmproj_pm]{predict.bpmproj_pm()}}
}
