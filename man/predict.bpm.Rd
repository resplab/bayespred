% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict.bpm}
\alias{predict.bpm}
\title{Predict from a bpm posterior object}
\usage{
\method{predict}{bpm}(
  object,
  newdata,
  type = c("response", "link", "terms"),
  method = c("pm", "pe", "pm_mackay"),
  se.fit = FALSE,
  interval = NULL,
  dispersion = NULL,
  na.action = na.pass,
  ...
)
}
\arguments{
\item{object}{A \code{bpm} object (obtained via \code{\link[=posterior]{posterior()}}).}

\item{newdata}{A data frame of new observations. Always required.}

\item{type}{Character: \code{"response"} (default) or \code{"link"}.}

\item{method}{Character: \code{"pm"} (default), \code{"pe"}, or \code{"pm_mackay"}.
Ignored when \code{type = "link"}.}

\item{se.fit}{Logical (default \code{FALSE}). If \code{TRUE}, return a list with
\code{fit}, \code{se.fit}, and \code{residual.scale}, matching \code{\link[stats:predict.glm]{stats::predict.glm()}}.}

\item{interval}{\code{NULL} (default), \code{0} (SE only, no bounds), or a coverage
probability in (0, 1). Cannot be combined with \code{se.fit = TRUE}.}

\item{dispersion}{Ignored; included for \code{\link[stats:predict.glm]{stats::predict.glm()}} compatibility.}

\item{na.action}{Function to handle \code{NA}s in \code{newdata}. Default
\link[stats:na.pass]{stats::na.pass}.}

\item{...}{Currently unused.}
}
\value{
See \code{\link[=predict.bpmfit]{predict.bpmfit()}} for return-value details.
}
\description{
Computes predictions from a \code{bpm} object (the deployable posterior obtained
via \code{\link[=posterior]{posterior()}}). Requires \code{newdata}; no training data is stored on a
\code{bpm} object. For in-sample predictions, call \code{\link[=predict.bpmfit]{predict.bpmfit()}} on the
original \code{bpmfit} instead.
}
\details{
The interface is intentionally compatible with \code{\link[stats:predict.glm]{stats::predict.glm()}}.
See \code{\link[=predict.bpmfit]{predict.bpmfit()}} for full parameter documentation.
}
\seealso{
\code{\link[=posterior]{posterior()}}, \code{\link[=predict.bpmfit]{predict.bpmfit()}}
}
