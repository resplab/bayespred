% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/projection.R
\name{project}
\alias{project}
\title{Compute projection coefficients from a bpm object}
\usage{
project(object, ...)
}
\arguments{
\item{object}{A \code{bpm} object.}

\item{...}{Currently unused.}

\item{formula}{Optional one- or two-sided formula specifying the projection
model. If \code{NULL} (default), self-projection onto the original design matrix
is performed.}

\item{data}{Data frame containing the development sample. Required for
custom projection; also required for self-projection when \code{object} was
fitted with \code{model = FALSE}.}

\item{family}{Family object for the projection fit. Defaults to
\code{object$family} (same link as the main model).}
}
\value{
A list with elements \code{coefficients}, \code{fit_method}, \code{terms},
\code{contrasts}, \code{xlevels}, and \code{family} — everything needed for
\code{predict(..., method = "pm_proj")} to reconstruct the design matrix on
new data.
}
\description{
Returns the projection result without modifying the object. For
self-projection (no \code{formula}), the posterior-mean predictions on the
development sample are regressed back on the same design matrix, producing
a simplified linear predictor. For custom projection, they are regressed on
a user-supplied (typically simpler) formula — useful for building clinical
risk scores with fewer or different predictors.
}
\details{
To cache the result on the object for use with
\code{predict(..., method = "pm_proj")}, pass the return value to
\code{\link[=add_projection]{add_projection()}} or use it directly in a pipe:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{fit <- bpm(...) |> add_projection()
fit <- bpm(...) |> add_projection(formula = ~ age + sex, data = dat)
}\if{html}{\out{</div>}}
}
\seealso{
\code{\link[=add_projection]{add_projection()}}, \code{\link[=predict.bpm]{predict.bpm()}}
}
